[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "shell-project-service"
version = "0.1.0"
description = "SHELL Project bounded context service"
requires-python = ">=3.14,<3.15"
dependencies = [
    "aiosqlite>=0.20.0",
    "alembic>=1.18.5",
    "dependency-injector>=4.49.1",
    "fastapi>=0.139.0",
    "pydantic>=2.13.4",
    "sqlalchemy>=2.0.51",
    "shell-platform>=0.1.0,<0.2.0",
    "saga-orchestration>=0.1.0,<0.2.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["shell.project_service*"]

[tool.uv.sources]
shell-platform = { path = "../shell-platform" }
saga-orchestration = { path = "../saga-orchestration" }

[tool.setuptools.package-data]
"shell.project_service.bootstrap.project" = ["config/*.yaml"]
"shell.project_service.migrations" = ["script.py.mako"]