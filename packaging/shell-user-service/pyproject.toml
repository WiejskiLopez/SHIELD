[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "shell-user-service"
version = "0.1.0"
description = "SHELL User bounded context service"
requires-python = ">=3.14,<3.15"
dependencies = [
    "aiosqlite>=0.20.0",
    "alembic>=1.18.5",
    "dependency-injector>=4.49.1",
    "fastapi>=0.139.0",
    "pydantic>=2.13.4",
    "sqlalchemy>=2.0.51",
    "shell-platform>=0.1.0,<0.2.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["shell.user_service*"]

[tool.setuptools.package-data]
"shell.user_service.bootstrap.user" = ["config/*.yaml"]
"shell.user_service.migrations" = ["script.py.mako"]

[tool.uv.sources]
shell-platform = { path = "../shell-platform" }